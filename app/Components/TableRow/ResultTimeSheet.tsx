'use client';

import { Row, Col } from 'react-bootstrap';
import { useState, useEffect } from 'react';

const ResultTimeSheet = (props: any) => {

    const [rowResultData, setRowResultData] = useState(
        props.data
    );

    return (
        <>
            <tr>

            </tr>
        </>
    )
}

export default ResultTimeSheet;